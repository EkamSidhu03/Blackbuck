// src/components/OrderList.js
import React from 'react';

const OrderList = ({ orders }) => {
  return (
    <div>
      <h2>Order List</h2>
      {/* Display order list using the 'orders' prop */}
    </div>
  );
};

export default OrderList;